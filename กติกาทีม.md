# 🤝 กติกาทีม — จีนรู้ใจ (2 คน)

> อัปเดต 8 ก.ค. 2026 · ไฟล์นี้คือข้อตกลงการทำงานร่วมกัน — ทั้งคนและ AI ผู้ช่วยของแต่ละคนต้องทำตาม

## 👥 การแบ่งงาน (ตามความถนัด — งานไม่ทับไฟล์กัน)

| 👨‍💻 บอล (วรเดช) — โค้ด/ระบบ | 👩‍🏫 หฤทัย — เนื้อหา/ภาษาจีน (HSK 5) |
|---|---|
| Next.js + FastAPI + Supabase | **คลังคำศัพท์ HSK 1–2** (ตรวจคำแปลไทยทุกคำ) |
| pyBKT + FSRS + Q-matrix (โค้ด) | **คลัง Thai-L1** (จุดผิดคนไทย — ตรวจความถูกต้องภาษา) |
| Deploy: Vercel / HF Spaces / Supabase | **Item bank ข้อสอบ** (AI ช่วยร่าง → คนตรวจเสมอ) |
| Mock Exam engine + Dashboard | ไฟล์เสียงข้อสอบฟัง + ตรวจภาษาในแอปทั้งหมด |

> งานไหนคาบเกี่ยว → บอกกันในแชทก่อนเริ่มทำ

## 🔄 กติกา Git (3 ข้อหลัก — ท่องให้ขึ้นใจ)

1. **`git pull` ก่อนเริ่มทำงานทุกครั้ง** (90% ของปัญหามาจากลืมข้อนี้)
2. **push บ่อย ๆ** — เสร็จเป็นชิ้นก็ push เลย ห้ามกั๊กงานไว้ในเครื่องข้ามวัน
3. **ห้ามแก้ไฟล์เดียวกันพร้อมกัน** — จะแก้ไฟล์ของอีกฝ่าย/ไฟล์กลาง (README, HANDOFF, proposal) ให้บอกกันในแชทก่อน

**ลำดับคำสั่งประจำวัน:**
```
git pull                          # 1. ดึงงานล่าสุด (ก่อนเริ่มเสมอ!)
... ทำงาน ...
git add .                         # 2. เตรียมไฟล์
git commit -m "สรุปสั้น ๆ ว่าทำอะไร"   # 3. บันทึก (เขียนให้อีกคนอ่านรู้เรื่อง)
git push                          # 4. ส่งขึ้น GitHub
```

**ถ้าเจอ conflict:** ไม่ต้องตกใจ — git ไม่ทับของใคร ให้เปิดไฟล์ที่ชน เลือก/รวมเนื้อหา แล้ว commit ต่อ (หรือโยนให้ AI ผู้ช่วยแก้ให้)

## 🔐 กติกาความปลอดภัย
- **ห้าม commit `.env` / API key เด็ดขาด** (มี `.gitignore` กันไว้ แต่ต้องไม่ฝืนใส่เอง)
- repo เป็น Private — ห้ามแชร์ลิงก์/เชิญคนนอกทีมโดยไม่คุยกัน

## 📁 กติกาไฟล์/โฟลเดอร์
- เอกสารทั้งหมดอยู่ใน `docs/` ตามหมวด `01–08` (ดู `README.md`) · โค้ดอยู่ `apps/ packages/ data/ ml/` — ไม่กองไฟล์ที่ root
- ไฟล์ "ล่าสุด/ตัวจริง" มีตัวเดียว ห้ามสร้างสำเนา `(1)`, `ฉบับใหม่2` — ถ้าจะแก้ใหญ่ให้แก้ในไฟล์เดิม (git จำประวัติให้อยู่แล้ว)
- เอกสารอ้างอิงหลักปัจจุบัน: `docs/01_เอกสารโครงงาน/ข้อเสนอโครงงาน-ฉบับแก้ไข-โฟกัสHSK.md` + `HANDOFF-จีนรู้ใจ.md`

## 🤖 กติกาการใช้ AI (สำคัญ — ใช้ทั้ง 2 เครื่อง)
1. เปิดโปรเจกต์นี้ใน Claude Code → AI อ่าน `.claude/CLAUDE.md` เองอัตโนมัติ (บริบทครบ ไม่ต้องอธิบายใหม่)
2. **ก่อนอ้างข้อเท็จจริง** (คู่แข่ง/งานวิจัย/ตัวเลข HSK) → ให้ AI **ตรวจสอบก่อน ห้ามเดา** (ทีมเคยพลาดเรื่อง Duolingo มาแล้ว)
3. **ห้ามแต่ง citation** — งานวิจัยต้องมีลิงก์จริง ใช้ agent `thai-l1-researcher` ค้น
4. สร้างข้อสอบใช้ skill `/hsk-item` → **แล้วหฤทัยตรวจภาษาก่อนเข้า item bank เสมอ** (AI ร่าง คนตัดสิน)
5. เนื้อหาที่ AI เขียน ทีมต้อง**เข้าใจและอธิบายได้** — เพราะคนตอบกรรมการคือเรา ไม่ใช่ AI

## 🎯 กติกาคุมสโคป (จาก feedback อาจารย์)
- โฟกัส **เตรียมสอบ HSK 1–2 (มาตรฐาน 3.0)** — 8 โมดูลตาม proposal ฉบับแก้ไข
- ตรวจคำตอบด้วย **rule เทียบเฉลยเท่านั้น** (ไม่ใช้ LLM ตรวจ)
- ของนอกสโคป (ออกเสียง/ASR, AI สนทนา, กล้อง, HSK 3+) = **future work** — ห้ามหลุดกลับเข้ามาใน MVP
- ถ้าเวลาไม่พอ ตัดจากท้าย ห้ามตัดแกน (คลังคำ, BKT, Thai-L1, FSRS, Mock Exam)

## 📅 จังหวะทีม
- อัปเดตกันสั้น ๆ ว่า "วันนี้ทำอะไร/ติดอะไร" ผ่านแชทเป็นประจำ
- ก่อนไปคุยอาจารย์ → เตรียมคำตอบจาก `HANDOFF-จีนรู้ใจ.md` หัวข้อ "เตรียมตอบกรรมการ"
- งานถัดไปดูที่ HANDOFF หัวข้อ "สถานะ + สิ่งที่ทำต่อ"
