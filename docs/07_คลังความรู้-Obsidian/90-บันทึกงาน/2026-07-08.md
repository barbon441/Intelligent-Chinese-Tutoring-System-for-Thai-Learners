---
tags: [บันทึกงาน]
updated: 2026-07-08
---

# 📅 บันทึกงาน 8 ก.ค. 2026

## ทำอะไรไปบ้าง
- รวมไฟล์จากเครื่องเพื่อน (หฤทัย) เข้า repo — เก็บ proposal ฉบับแก้ไข-โฟกัสHSK (ตัวล่าสุด) + บทพูดนำเสนอ + ลบไฟล์ซ้ำ (ตรวจ hash แล้ว)
- อัปเดต `HANDOFF-จีนรู้ใจ.md` เป็นทิศทาง HSK เต็มรูปแบบ (ดู [[ปรับขอบเขต-โฟกัสHSK]])
- วิเคราะห์ mockup 2 ไฟล์จากอาจารย์ (อ่านโค้ด + รัน Playwright) → ดู [[วิเคราะห์-mockup-อาจารย์]]
- เขียน `กติกาทีม.md` (แบ่งงาน + กติกา git + กติกาใช้ AI) + ผูกเข้า `.claude/CLAUDE.md`
- สร้าง vault คลังความรู้นี้ (โน้ตตั้งต้น 16 เรื่อง)

## การตัดสินใจสำคัญ
- ใช้ **ไฟล์ 9** ของ mockup อาจารย์เป็นตัวอ้างอิง (ใหม่กว่าไฟล์ 8)
- จุดที่ต้องคุยกับอาจารย์: mockup มีข้อสอบพูด 10/25 แต่ HSK 1–2 ไม่มีสอบพูด → เสนอแทนด้วย minimal pair ทางการฟัง (ดู [[รูปแบบข้อสอบ-HSK-1-2]])

## เพิ่มเติมช่วงค่ำ: ร่าง PRD + ARCHITECTURE เสร็จ
- สร้าง `docs/08_สเปค-พัฒนา/PRD.md` (โมดูล 8 ตัว + AC + P0/P1 + user flows + milestone ราย 2 สัปดาห์ M1–M8 + ความเสี่ยง + open questions 8 ข้อ) และ `ARCHITECTURE.md` (diagram + Dexie/sync + FastAPI modules + data model 10 ตาราง + RLS + ML pipeline + deployment + โครง monorepo)
- ผ่านการตรวจขัดแย้งแล้ว: **ไม่มี LLM ตรวจข้อสอบ / ไม่มี ASR ใน MVP / ตัวเลข HSK 3.0 ถูกทุกจุด** — แก้ 6 ประเด็นจาก verifier (เช่น Mock Exam ต้องเสิร์ฟข้อสอบแบบไม่มีเฉลย, เพิ่ม exam_sessions ใน Dexie ให้ทำต่อได้หลังรีเฟรช, ablation เป็นสมมติฐานไม่ใช่เกณฑ์ผ่านตก)
- การตัดสินใจสำคัญ: Mock Exam ครั้งแรก = ทั้ง placement และ pre-test · ลำดับ phase = HSK 1 ครบวงจรก่อนแล้วขยาย HSK 2

## เพิ่มเติม: ย้ายเอกสารเข้า docs/
- ย้ายโฟลเดอร์ 01–08 ทั้งหมดเข้า `docs/` (ตามคอนเวนชันโปรเจกต์ซอฟต์แวร์) — root เหลือ README/HANDOFF/กติกาทีม/.claude + ที่ว่างสำหรับโค้ด (`apps/ packages/ data/ ml/`)
- แก้ reference ครบทุกไฟล์ (ใช้ git mv ประวัติไม่หาย)
- ⚠️ **Obsidian vault path เปลี่ยน**: เปิด vault ใหม่ที่ `docs/07_คลังความรู้-Obsidian`

## งานถัดไป (เรียงความสำคัญ)
1. ทีมรีวิว PRD/ARCHITECTURE (โดยเฉพาะ open questions 8 ข้อ + assumptions ที่ flag ไว้)
2. เติมชื่อผู้จัดทำ/ที่ปรึกษา + เอกสารอ้างอิง 1.9 ใน proposal → ทำลง .docx
3. ตารางแมป skill / Q-matrix ตั้งต้น (ดู [[จุดผิดคนไทย]] + [[pyBKT]])
4. เริ่ม scaffold โค้ดตาม ARCHITECTURE (M1: ดู [[Next.js-PWA]] + [[FastAPI]] + [[Supabase]])
