---
tags: [บันทึกงาน]
updated: 2026-07-12
---

# 📅 บันทึกงาน 12 ก.ค. 2026

## ทำอะไรไปบ้าง
- ✅ ปิด proposal 1.9: เติม references ครบ 15 รายการ (ตรวจสอบแล้ว — ดู [[งานวิจัยอ้างอิง]])
- ✅ **Scaffold โค้ดตาม M1** (ตาม [[ปรับขอบเขต-โฟกัสHSK]] + ARCHITECTURE):
  - `apps/web` — Next.js + TS + Tailwind (หน้าแรกจีนรู้ใจ + ปุ่มเช็ก API) — **build ผ่าน**
  - `apps/api` — FastAPI (/health, /docs) + Dockerfile พอร์ต 7860 สำหรับ HF Spaces — **รันจริง /health ตอบ ok**
  - `packages/shared` — constants.ts/.py (PASS_SCORE=120, HSK_LEVELS, THAI_L1_GROUPS)
  - `data/ ml/ scripts/` โครง + README · `.github/workflows/keepalive.yml` (cron ปลุก HF+Supabase)
  - `.env.example` ทั้ง 2 ฝั่ง (ค่าจริงห้าม commit)

## งานถัดไป
1. คุณบอล: สมัคร/ตั้งค่า **Supabase + Vercel + HF Spaces** → deploy scaffold ขึ้นจริง
2. M2: นำเข้า HSK 3.0 wordlist + CC-CEDICT → แปลไทย (หฤทัยตรวจ) → seed Supabase
3. ร่าง Thai-L1 catalog ฉบับแรก + Q-matrix ตั้งต้น
4. เริ่มชวนผู้ทดสอบ (คอขวดจริง!)