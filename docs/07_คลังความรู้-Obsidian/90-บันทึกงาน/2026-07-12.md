---
tags: [บันทึกงาน]
updated: 2026-07-12
---

# 📅 บันทึกงาน 12 ก.ค. 2026

## ทำอะไรไปบ้าง
- ✅ ปิด proposal 1.9: เติม references ครบ 15 รายการ (ตรวจสอบแล้ว — ดู [[งานวิจัยอ้างอิง]])
- ✅ **Scaffold โค้ดตาม M1** (ตาม [[ปรับขอบเขต-โฟกัสHSK]] + ARCHITECTURE):
  - `apps/web` — Next.js + TS + Tailwind (หน้าแรกจีนรู้ใจ + ปุ่มเช็ก API) — **build ผ่าน**
  - `apps/api` — FastAPI (/health, /docs) + Dockerfile พอร์ต 7860 สำหรับ HF Spaces — **รันจริง /health ตอบ ok**
  - `packages/shared` — constants.ts/.py (PASS_SCORE=120, HSK_LEVELS, THAI_L1_GROUPS)
  - `data/ ml/ scripts/` โครง + README · `.github/workflows/keepalive.yml` (cron ปลุก HF+Supabase)
  - `.env.example` ทั้ง 2 ฝั่ง (ค่าจริงห้าม commit)

## งานถัดไป
1. คุณบอล: สมัคร/ตั้งค่า **Supabase + Vercel + HF Spaces** → deploy scaffold ขึ้นจริง
2. M2: นำเข้า HSK 3.0 wordlist + CC-CEDICT → แปลไทย (หฤทัยตรวจ) → seed Supabase
3. ร่าง Thai-L1 catalog ฉบับแรก + Q-matrix ตั้งต้น
4. เริ่มชวนผู้ทดสอบ (คอขวดจริง!)
## เพิ่มเติม: M2 เริ่มแล้ว — คลังคำ HSK 1 (300 คำ) พร้อมคำแปลไทยฉบับร่าง
- ⚠️ เจอกับดักจริง: dataset "new-1" = band มาตรฐาน (506 คำ) ไม่ใช่ลิสต์ข้อสอบ → แก้โดยดึงลิสต์สอบ 300 คำ ยืนยันด้วยการ intersect 2 แหล่งอิสระ (mandarinbean ∩ hsk1.org = 300 เป๊ะ) — ดู [[HSK-3.0-ข้อเท็จจริง]]
- สร้าง `data/wordlist/hsk30-level1-v2026.07.json` (300 คำ: พินอิน + อังกฤษ CC-CEDICT + **ไทยฉบับร่าง AI ครบ 300**)
- แปลไทยด้วย AI 6 ชุดขนาน + ตัวตรวจทานแก้ 13 คำ (ส่วนใหญ่เรื่องรูปแบบตัวเลขให้สม่ำเสมอ)
- 📋 **รอหฤทัยตรวจ**: `data/wordlist/REVIEW-hsk1-แปลไทย-สำหรับหฤทัย.md` (300 แถว) — th_reviewed ยังเป็น false ทุกคำ
- สร้าง SQL: `data/seeds/001_create_words.sql` (ตาราง words + RLS) + `002_seed_words.sql` (300 แถว upsert) → รอ paste ลง Supabase SQL Editor
- สคริปต์ pipeline ถาวร: `scripts/build_wordlist.py` + `scripts/make_seed.py`
## งานค้างเพิ่ม: ยืนยันลิสต์ 300 คำกับแหล่งทางการ
- ลิสต์ปัจจุบันยืนยันด้วย 2 แหล่งอิสระ intersect = 300 เป๊ะ (mandarinbean ∩ hsk1.org)
- ⬜ ทีมเปิด chinesetest.cn (หน้า HSK 1 → 词汇) ในเบราว์เซอร์เอง แล้ว spot-check ~20-30 คำ + เซฟ PDF/screenshot ทางการเก็บใน data/ เป็นหลักฐาน (automation เข้าไม่ได้ — เว็บช้า/กันบอท)
## ค้นพบสำคัญ: เปิดหน้า chinesetest.cn จริง (12 ก.ค.) — ยังเป็นระบบเก่า
- หน้า HSK 1 ทางการยังแสดง "total 150" คำ (หลักสูตร 2.0 เดิม — ยังมี 北京) → **ยืนยันว่ายังไม่สลับเป็น 3.0** ตรงตามที่คาด (~18 ก.ค.)
- อัปเดตไฟล์ `แหล่งที่มา-คำศัพท์HSK1.md`: เตือนห้ามใช้หน้าทางการยืนยันเลข 300 จนกว่าจะสลับระบบ + เพิ่มเหตุผล "ทำไมเตรียมระบบใหม่ไม่ใช่ 150 เก่า"
- ⬜ งานหลัง 18 ก.ค.: เปิดหน้าทางการใหม่ → ควรเป็น 300 → แคปเก็บหลักฐาน + spot-check ดู [[HSK-3.0-ข้อเท็จจริง]]
## เพิ่มเติม: seed 300 คำ HSK1 ขึ้น Supabase สำเร็จ ✅
- ตั้ง Supabase (icts-thai, Singapore ap-southeast-1) + ต่อ DB ผ่าน Session pooler
- สร้างตาราง `words` (+ RLS public-read) + seed 300 คำครบ (ยืนยันจาก DB: 300 แถว, meaning_th ครบ 300)
- เครื่องมือใหม่: `scripts/db_apply.py` — รันไฟล์ SQL กับ Supabase ได้จาก AI โดยตรง (ใช้ SUPABASE_DB_URL) → ตารางต่อไปไม่ต้อง paste SQL เอง
- 🐛 เกร็ดแก้ปัญหา: connection string ที่รหัสผ่านมี `@` ต้อง encode เป็น `%40` (ไม่งั้น host เพี้ยน)

## งานถัดไปทันที
1. pre-generate เสียง 300 ไฟล์ (edge-tts) → Supabase Storage
2. M3: หน้าบัตรคำ (ดึงจาก Supabase) + ปุ่มตรวจคำแปลสำหรับหฤทัย (ตรวจสะสมจนครบ 300)
## เพิ่มเติม: เสียงอ่าน 300 คำ เสร็จ (M2 ปิดสมบูรณ์) 🔊
- สร้าง mp3 300 ไฟล์ด้วย edge-tts (zh-CN-XiaoxiaoNeural, rate -10% เหมาะผู้เริ่มต้น) → อัปขึ้น Supabase Storage bucket "audio" (public) → อัปเดต audio_path ครบ 300/300
- ยืนยันแล้ว: เล่นจาก public URL ได้จริง + DB มี audio_path ครบ
- 🐛 เกร็ด: (1) edge-tts ใช้ได้ปกติ — ที่ fail ตอนแรกเพราะตัวอักษรจีนหายตอน pipe ผ่าน PowerShell stdin (แก้โดยเขียนเป็นไฟล์ .py) (2) ต้อง edge-tts >=7.x
- เครื่องมือ: `scripts/generate_audio.py` (รันซ้ำได้ · ไฟล์เสียง local อยู่ใน .gitignore เพราะ source of truth = Storage)

## ✅ สรุป M2 เสร็จ: คลังคำ HSK1 พร้อมใช้เต็มรูป (300 คำ + พินอิน + ไทย + เสียง) → พร้อมทำหน้าบัตรคำ (M3)
## เพิ่มเติม: M3 หน้าบัตรคำ เสร็จ + ทดสอบผ่าน 🃏
- `apps/web/src/lib/supabase.ts` — ต่อ Supabase (anon read) + helper audioUrl
- `apps/web/src/app/flashcards/page.tsx` — บัตรคำ: ฮั่นจื้อใหญ่ + พินอิน + พลิกดูคำแปลไทย + ปุ่มฟังเสียง (เล่นจาก Storage) + เลื่อนก่อน/ถัดไป + คีย์ลูกศร/สเปซ + progress bar
- ปุ่มตรวจคำแปลสำหรับหฤทัย (✓ ถูก / ✏️ แก้) → เขียนผ่าน API endpoint ใหม่ `POST /words/{id}/review` (service-side, ตาราง words client เขียนไม่ได้)
- ✅ ทดสอบ Playwright ผ่านหมด: โหลด 300 คำจาก Supabase, พลิกบัตร (爱=รัก/ชอบ), เลื่อนถัดไป (1→3/300), กดตรวจ → progress 0→1/300 (เขียน DB จริงผ่าน API), ไม่มี console error

## 🎉 ตอนนี้แอปใช้งานได้จริงครั้งแรก! (เว็บ ↔ API ↔ Supabase ครบวงจร)
งานถัดไป: M3 ต่อ (FSRS คิวทวน) หรือ M4 (Learner Model + log การตอบ)
## เพิ่มเติม: ทำ App Shell (หน้าตาแอปจริง) 🎨
- `apps/web/src/components/AppShell.tsx` — Header แบรนด์ (จีนรู้ใจ 中文知心) + เมนูล่างแบบแอปมือถือ (หน้าแรก/บัตรคำ/ฝึก/ผล/โปรไฟล์ · โมดูลยังไม่ทำขึ้น "เร็ว ๆ นี้")
- `apps/web/src/app/page.tsx` — หน้าแรก Dashboard จริง (แทนหน้าเช็ก API เดิม): การ์ดต้อนรับ 你好 + สถิติคลังคำสด (300 คำ/300 เสียง/ตรวจแล้ว X) ดึงจาก Supabase + รายการโมดูล
- `layout.tsx` — ฟอนต์ IBM Plex Sans Thai + Noto Sans SC + ธีมหมึกจีน (ฟ้า-ขาว-ชมพู) ตาม mockup
- ปรับหน้าบัตรคำให้อยู่ใน shell (ตัดกรอบเต็มจอเดิม)
- ✅ build ผ่าน + Playwright แคปหน้าจอ: dashboard + บัตรคำ สวยงาม ไม่มี error (ที่ test เด้งคือปุ่ม dev-tools บังเมนู ไม่ใช่บั๊ก)
- เพิ่ม `dev.bat` — ดับเบิลคลิกเปิด API+เว็บ+เบราว์เซอร์ ทีเดียว

## 🎉 แอปมีหน้าตาจริงแล้ว (App Shell + Dashboard + บัตรคำ) — เติมโมดูลอื่นลงใน shell นี้ได้เลย
## ⚠️ ค้นพบ: Hugging Face Docker Space กลายเป็นของเสียเงิน (ก.ค. 2026)
- ตอนจะ deploy API พบว่า HF Docker Space ต้อง PRO แล้ว (มีแต่ Static ฟรี ซึ่งรัน FastAPI ไม่ได้) — เพิ่งเปลี่ยน ~4 วันก่อน (ยืนยันจากฟอรัม HF + หน้าสร้าง Space ขึ้น "Paid")
- **เปลี่ยนแผน backend: HF Spaces → Render free web service** (512MB ฟรี ไม่ต้องบัตร connect GitHub auto-deploy) · 512MB พอเพราะ pyBKT เทรน offline ไม่ใช่บน server
- อัปเดตแล้ว: HANDOFF §5 + [[โฮสติ้งฟรี]] (ดู [[FastAPI]])
- 🔑 บทเรียนย้ำ: เงื่อนไข "ฟรี" เปลี่ยนเร็วมาก แม้แต่ที่เราเช็กไว้แล้ว — ต้องเช็กสดตอนจะใช้จริง
## 🎉 Deploy API สำเร็จ! (Render)
- API live: https://intelligent-chinese-tutoring-system-for.onrender.com
- ทดสอบผ่าน: /health 200 · / 200 · POST /words/1/review 200 (ต่อ Supabase เขียน DB ได้จริงจากคลาวด์)
- Render: Docker (auto-detect Dockerfile), region Singapore, Free 512MB, Root=apps/api, auto-deploy on push (paths apps/api/**), health check /health, secrets 3 ตัวใส่ในหน้า Render
- สร้าง `SETUP.md` — วิธีเริ่มรันสำหรับทีม (เครื่องแรง=dev.bat · เครื่องช้า=รันเว็บ ชี้ API คลาวด์)
- 🎯 option B พร้อม: เพื่อน (เครื่องช้า) รันแค่ npm run dev + ตั้ง NEXT_PUBLIC_API_URL=คลาวด์ → เบา+ครบ

## ✅ Milestone: มี API บนคลาวด์แล้ว — เว็บ local ชี้ไปได้ทั้งเครื่องแรง/ช้า · ถัดไป M4 (FSRS) หรือ deploy เว็บ (Vercel)
## 🎉🎉 Deploy เว็บสำเร็จ (Vercel) — ระบบทั้งหมด live บนคลาวด์! (บอล)
- เว็บ live: https://intelligent-chinese-tutoring-system.vercel.app
- Vercel: Root=apps/web, env 3 ตัว (API URL ชี้ Render + Supabase URL/anon), auto-deploy on push
- ทดสอบผ่าน Playwright บนเว็บจริง: dashboard โหลด, บัตรคำ 300 คำจาก Supabase, ไม่มี console error, สถานะ th_reviewed อ่านถูก
- แก้ปัญหาเครื่องเพื่อนดับ: npm run dev หนัก (กิน RAM) → เพื่อนเปิดแค่ URL Vercel ในเบราว์เซอร์ (เบามาก ไม่ต้องรันอะไร)
- อัปเดต SETUP.md ใส่ URL เว็บ

## ✅ สถาปัตยกรรม deploy ครบ: Vercel (เว็บ) → Render (API) → Supabase (DB+เสียง) ทั้งหมดฟรี · push → auto-deploy ทั้งคู่

## เพิ่มเติม (เครื่องหฤทัย): setup เครื่อง + ทางลัด npm run dev ที่ราก 💻
- setup เครื่องหฤทัยครบ: venv + npm ci + .env 2 ฝั่ง → ทดสอบรันจริงผ่าน (เว็บโหลดบัตรคำจาก Supabase, API local ตอบ /health)
- ⚠️ เครื่องหฤทัยไม่ทนโหลดหนัก: Windows log มีดับกะทันหันย้อนถึง พ.ค. (ไม่มี bugcheck — น่าจะความร้อน) + `next dev` เคยตาย out-of-memory ตอน compile แรก (pagefile พีค 10.7GB) → ใช้สูตร "เครื่องช้า" ใน SETUP.md: รันแค่เว็บ + ชี้ API ไป Render · compile รอบถัดไปเบาลงมากเพราะมีแคช
- เพิ่ม `package.json` ที่รากโปรเจกต์ → พิมพ์ `npm run dev` ที่รากได้เลย ไม่ต้อง cd apps/web (ทดสอบผ่านแล้ว)
- 🔒 เกร็ดความปลอดภัย: ไฟล์ env ที่ก็อปข้ามเครื่องแบบไม่มีจุดนำหน้า (`env`, `env.local`) **ไม่ถูก .gitignore คุ้ม** เสี่ยงหลุดขึ้น GitHub — เปลี่ยนชื่อเป็น `.env`/`.env.local` แล้ว + เพิ่ม `.playwright-mcp/` ลง .gitignore

## 🧩 สร้าง 3 โมดูลใหม่ (ฝึก / ผล / โปรไฟล์) — บอล (ระหว่างเพื่อนไปทานข้าว)
- **/practice (ฝึก):** ควิซสร้างข้อสอบอัตโนมัติจากคลัง 300 คำ · 2 โหมด: อ่าน (เห็น 汉字+พินอิน→เลือกคำแปล) / ฟัง (ฟังเสียง→เลือก 汉字) · 10 ข้อ/รอบ · **ตรวจด้วย rule (เทียบ id) ไม่ใช้ LLM** ตรงแนวอาจารย์ · feedback ทันที + หน้าสรุปคะแนน
- **/progress (ผล):** แดชบอร์ด — ความแม่นยำรวม, รอบที่ฝึก, streak, แถบคลังคำ (300/300 เสียง, ตรวจแล้ว x/300), ประวัติการฝึกล่าสุด
- **/profile (โปรไฟล์):** สรุปการเรียน + เป้าหมาย HSK 1 (120/200) + เครดิตทีม
- เก็บผลการฝึกใน localStorage (`src/lib/progress.ts`) ไปก่อน — พอมี Auth+BKT (M4+) ค่อยย้ายขึ้น Supabase รายบุคคล (โครงข้อมูลออกแบบให้ต่อยอด BKT/FSRS ได้)
- **ไม่เพิ่ม dependency ใด ๆ** (React state + localStorage ล้วน) → เครื่องเพื่อนไม่ต้อง install เพิ่ม + เบา
- ทดสอบครบ: `next build` ผ่าน (8 routes) + Playwright ยิงจริง (เมนู→ทำ 10 ข้อ→สรุปผล→ประวัติบันทึก→โปรไฟล์) **ไม่มี console error**
- เปิดปุ่มเมนูล่าง ฝึก/ผล/โปรไฟล์ = ready + หน้าแรกลิงก์การ์ดโมดูลใหม่
- เหลือทำ: FSRS (ทบทวนอัจฉริยะ) + Mock Exam จับเวลา + BKT/Thai-L1 (M4+)
## 🔁 โมดูล 4: FSRS ทบทวนอัจฉริยะ เสร็จ (เวอร์ชันแรก) — ปิดแกน learn→review ✅
- ติดตั้ง **ts-fsrs 5.4.1** (FSRS v6 ตัวจริงตาม PRD) · `apps/web/src/lib/fsrs.ts` + หน้า `apps/web/src/app/review/page.tsx`
- คิว "ต้องทวนวันนี้" = การ์ด due (คำนวณจากสถานะ D/S/R) + คำใหม่วันละ ~10 (กันเด้ง 300 คำรวด)
- UX แบบ Anki: การ์ดพลิกดูคำแปล + **ปุ่ม 4 ระดับโชว์ช่วงเวลาที่ FSRS คำนวณจริง** (คำใหม่: ลืม 1น · ยาก 6น · ปกติ 10น · ง่าย 7วัน)
- หน้าแรกเพิ่มปุ่ม "🔁 ทวนวันนี้ (N)" นับคิวสด + เปิดการ์ด FSRS ในรายการโมดูล
- แยกบทบาทชัดตาม PRD: FSRS = "ทวนเมื่อไหร่" (ยังไม่ปนกับ BKT = "แม่นแค่ไหน")
- เก็บสถานะการ์ดใน localStorage ก่อน (ออฟไลน์ได้ทันที) → sync Supabase รายบุคคลไว้ทำตอนมี Auth
- ทดสอบครบ: `next build` (9 routes) + Playwright (start→พลิก→ให้คะแนนครบ 10→จบรอบ→reload คงกำหนด) ไม่มี console error · ยืนยันบน Vercel
- อัปเดตโน้ต [[FSRS]] (เพิ่มสถานะ "ทำจริงแล้ว")

## 📌 สถานะแกน P0: บัตรคำ ✅ · FSRS ✅ · เหลือ pyBKT (M2) + Thai-L1 (M3) + Mock Exam (M7) · จุดต่อไปที่คุ้ม = data pipeline (Auth+log) เพื่อปูทาง BKT + เริ่ม Thai-L1 catalog (หฤทัย+agent)
## 🩺 แก้ dev server ดับเงียบ ๆ (เครื่องบอล) — ใส่เพดานแรม Node
- วินิจฉัยจากเครื่องจริง: RAM 16GB แต่ available เหลือ ~2.5GB (Chrome 3.9GB/31 proc + VS Code 2GB + Discord + Steam กินเต็ม) → dev spike ตอน compile แล้วโดน Windows OOM-kill เงียบ ๆ (ไม่มี error = ถูกฆ่าจากข้างนอก ไม่ใช่บั๊กโค้ด)
- ข้อเท็จจริง: Next dev + Turbopack เองกินปานกลาง (~0.6–1.5GB) ไม่ใช่ตัวหลัก — ตัวหลักคือ Chrome/VSCode/Discord/Steam
- แก้: `apps/web/package.json` dev script → `set NODE_OPTIONS=--max-old-space-size=2048 && next dev` (cap heap 2GB กันบวมไม่หยุด · ทดสอบแล้ว default 4288MB → 2240MB) · ทั้งทีมได้ผลอัตโนมัติ ไม่ต้องพิมพ์ env เอง
- ⚠️ ยาหลักยังคือ "ปิด Chrome แท็บเยอะ + Discord + Steam ตอนโค้ด" (คืน ~4–5GB) · cap เป็นแค่ตัวเสริม
## 🧠 Thai-L1 Error Catalog v1 (ร่าง) เสร็จ — จุดขายตัวจริง (โมดูล 3) ⭐
- รัน workflow (20 agents, 0 error, ~15 นาที): 3 agent ค้นวิจัยขนาน (thai-l1-researcher) + ตรวจ citation แบบ adversarial (general-purpose)
- ได้ **15 KC** (5×3 กลุ่ม: วรรณยุกต์ / retroflex / ไวยากรณ์) แต่ละ KC มี จุดผิด/สาเหตุ L1/ตัวอย่าง HSK1/ไอเดียตัวลวง/วิธีสอน/หลักฐาน · **17 sources ตรวจ citation แล้ว**
- ไฟล์: `data/thai-l1/catalog-v1.json` (+ Q-matrix seed 15 KC) · `REVIEW-thai-l1-สำหรับหฤทัย.md` (รอตรวจ) · `citation-audit.md` (ผลตรวจเต็ม + citation ฉบับแก้)
- 🔴 **การตรวจ adversarial จับของสำคัญ:** citation เดิม "Chankrajang 2022 — 补语 ผิด 48%" **ยืนยันไม่ได้เลย** (หาไม่เจอทั้งชื่อ/วารสาร/ตัวเลข) → เปลี่ยนเป็น **Pichaichanlert 2022** + **ตัด 48% ทิ้ง** (⚠️ ตัวเลขนี้อยู่ใน PRD/proposal ด้วย ต้องแก้) · Rungruang ต้องเพิ่ม **& Mu** + หน้า **107**
- แปลง JSON→ไฟล์ด้วยสคริปต์ (faithful 100% ไม่พิมพ์มือ กัน citation เพี้ยน)
- อัปเดตโน้ต [[จุดผิดคนไทย]] + [[งานวิจัยอ้างอิง]]
- ⬜ ถัดไป: หฤทัยตรวจ REVIEW · แก้ citation ในเล่ม (48%/Rungruang) · เอา KC ไปทำ Q-matrix + distractor จริงในข้อสอบ
## 🔧 แก้กลับ: Chankrajang/48% เป็นของจริง (ผมพลาดเองเมื่อกี้)
- ตอนสรุป catalog ผมเชื่อ summary ของ research agent ("หา Chankrajang/48% ไม่เจอ → ตัดทิ้ง/ใช้ Pichaichanlert แทน") เร็วไป → แก้โน้ต + บอกบอลผิดว่า "ตัด 48%"
- แต่ชั้นตรวจ adversarial (verify agent) + ผมเปิดหน้า TCI จริง (WebFetch) ยืนยัน: **Chankrajang 2022 / 48.07% เป็นของจริง 100%** (นพพล จันทร์กระจ่างแจ้ง, *Suan Sunandha Asian Social Science* 13(1) 64–77, n=169, 6 โรงเรียน) — Pichaichanlert เป็น**คนละงาน** (เสริม ไม่ใช่แทน)
- ย้อนแก้: `งานวิจัยอ้างอิง.md`, `จุดผิดคนไทย.md` (เก็บ 48.07% + บริบท), `PRD.md` (เติมทศนิยม+บริบท), REVIEW (แบนเนอร์เตือนหฤทัย) · proposal ref #14 + Rungruang ในเล่ม**ถูกอยู่แล้ว** ไม่ต้องแก้
- ⚠️ `catalog-v1.json`: summary กลุ่ม TL-GRAM ยังมีข้อความ research agent (ผิด) ฝังอยู่ — flag ไว้ regen รอบหน้า (KC/ข้อมูลอื่นถูกหมด)
- 🔑 บทเรียน: "agent หาไม่เจอ ≠ ไม่มีจริง" · ชั้น verify แบบ adversarial คุ้มมาก (จับ false-negative ของ research agent ได้) · ก่อนตัด citation ต้องเปิดแหล่งตรงยืนยันเอง (กฎ "ตรวจก่อนอ้าง")

## เพิ่มเติม (เครื่องหฤทัย): หฤทัยตรวจคำแปลไทยครบ 300/300 ✅ + เตรียมพบอาจารย์ 13 ก.ค.
- หฤทัย**ตรวจคำแปลครบทั้งไฟล์** `REVIEW-hsk1-แปลไทย-สำหรับหฤทัย.md` → AI ซิงก์ผลเข้า wordlist JSON + Supabase แล้ว: **th_reviewed 300/300**
- แก้ตามผลตรวจ: **边** = "ข้าง, ด้าน, ขอบ" (เพิ่ม "ขอบ") — อัปเดตทั้ง JSON และ DB แล้ว
- **家人** = "คนในครอบครัว" คงเดิม (หฤทัยตัดสินแล้ว — ไม่เพิ่ม "ครอบครัว" เพื่อไม่ให้ชนกับบัตร 家 ที่แปลว่า "บ้าน / ครอบครัว" อยู่แล้ว)
- บทเรียน workflow: การตรวจในไฟล์ .md ไม่อัปเดต DB อัตโนมัติ — ต้องกด ✓/✏️ ในแอป หรือให้ AI ซิงก์จากไฟล์แบบครั้งนี้
- 📅 อาจารย์นัดตรวจ**ทุกวันจันทร์** เริ่มพรุ่งนี้ (13 ก.ค.) + มี**สอบ 50%** ประมาณ ~ต้น ส.ค. (นับ 1 เดือนจาก 1 ก.ค. — วันจริงรอถามอาจารย์พรุ่งนี้)
- แผนพรุ่งนี้: เดโมของที่มี (Dashboard + บัตรคำ + ฝึก + ทวน FSRS — เปิดจากเว็บ Vercel ได้เลย, ปลุก Render ก่อนเข้าพบ) + ถามให้ชัด: วันสอบ 50% / ต้องส่งเล่มกี่บท / เกณฑ์ให้คะแนน
- 📋 งานใหม่รอหฤทัย: ตรวจ `data/thai-l1/REVIEW-thai-l1-สำหรับหฤทัย.md` (คลัง Thai-L1 15 KC ที่บอลร่างด้วย agent)

## เพิ่มเติม: setup เครื่องใหม่ (Windows 11, D:\Project_Jeen_roo_jai) + วิเคราะห์ทั้งโปรเจกต์ 🖥️
- โคลน repo สำเร็จ (ล็อกอิน GitHub ผ่าน Git Credential Manager OAuth — บัญชี 407-haluethai) · npm install + Python 3.12 venv + pip ครบ · Node v20.15.1 ผ่านเกณฑ์ Next 16 (ต้อง >= 20.9)
- 💡 เทคนิคใหม่: สร้าง `apps/web/.env.local` ได้**โดยไม่ต้องขอไฟล์จากบอล** — ดึงค่า public (NEXT_PUBLIC_SUPABASE_URL + ANON_KEY) จาก JS bundle ของเว็บ Vercel ที่ deploy แล้ว (ค่าพวกนี้ฝังใน client bundle เป็น public โดยดีไซน์อยู่แล้ว)
- ตั้ง `NEXT_PUBLIC_API_URL` ชี้ Render (สูตรเครื่องช้า — ฟีเจอร์ครบรวมโหมดตรวจ เพราะ Render มี secret ครบ) · ถ้าจะ dev ฝั่ง API ในเครื่อง ให้สลับ comment เป็น localhost:8000 ใน .env.local
- ทดสอบผ่านครบ: เว็บ :3000 render, API local :8000 /health ok, Render /health ok, Supabase อ่าน words ได้ (300 คำ th_reviewed+เสียงครบ 300/300)
- ⬜ ค่าเดียวที่ยังขาดในเครื่องนี้: `SUPABASE_DB_URL` ใน `apps/api/.env` (ขอจากบอล — จำเป็นเฉพาะตอนเทส API เขียน DB ในเครื่อง/รัน scripts) · เติมแล้วต้อง restart uvicorn (main.py อ่านตอน import, --reload ไม่ watch .env)
- ~~🔴 ระเบิดเวลา: `data/seeds/002_seed_words.sql` stale (th_reviewed=false ทั้ง 300)~~ → **ปลดแล้ว ✅**: พิสูจน์ก่อนว่า wordlist JSON ↔ DB จริงตรงกัน 100% (เทียบทีละคำทั้ง 300 × 8 คอลัมน์ที่ seed ทับ — 0 จุดต่าง) แล้ว regenerate ด้วย `make_seed.py` · ตรวจไฟล์ใหม่: true 300/300, 边 = "ข้าง, ด้าน, ขอบ" (ผลตรวจหฤทัยอยู่ครบ), ไม่แตะ audio_path · ตอนนี้รัน `db_apply.py` ซ้ำ = idempotent ปลอดภัย · 🔑 บทเรียน: seed เป็นไฟล์ generate — **แก้ข้อมูลใน DB/JSON เมื่อไร ต้องรัน make_seed.py ตามทันที** (หรือเลิก commit seed แล้ว generate ตอนใช้)
- 📝 พบเอกสารไม่ตรงโค้ด (รอแก้): `.env.example` ไม่มี SUPABASE_DB_URL (ตัวเดียวที่ API อ่านจริง) · ARCHITECTURE.md ใช้ชื่อ NEXT_PUBLIC_API_BASE_URL (ของจริงคือ NEXT_PUBLIC_API_URL) · data/README.md อ้าง item-bank/ + thai-l1-catalog/ ที่ไม่มีจริง · Thai-L1 group ID สองมาตรฐาน (constants.py vs catalog-v1.json) ต้อง map ก่อนทำ Q-matrix
- ⚠️ ความปลอดภัยที่ควรรีบทำเมื่อเริ่ม M4/Auth: `POST /words/{id}/review` บน Render ไม่มี auth + CORS * — ใครรู้ URL ก็เขียนทับคำแปลได้ (ตอนนี้มีของจริงให้พังแล้ว 300 คำ)

## 🧩 โหมด "เรียงประโยค" (排列顺序) เสร็จ — โหมดที่ 3 ของหน้าฝึก
- สร้างชุดประโยค HSK1 ฉบับร่าง 10 ประโยค (`apps/web/src/data/sentences.ts`) ใช้เฉพาะคำใน 300 คำ + 4 ประโยคดักจุดผิดลำดับคำคนไทย (的-order, ลักษณนาม, 也, 都 ผูก Thai-L1)
- UI: `apps/web/src/app/practice/SentenceOrder.tsx` — แตะคำมาเรียง → **ตรวจด้วย rule (เทียบลำดับ token) ไม่ใช้ LLM** → เฉลย+พินอิน+โน้ตไวยากรณ์ · บันทึกผลเข้าหน้า "ผล" (mode "order")
- wire เข้าหน้าฝึกเป็นการ์ดที่ 3 + อัปเดต progress page แสดง "เรียงประโยค"
- ⏳ **รอหฤทัยตรวจ**: `data/sentences/REVIEW-เรียงประโยค-สำหรับหฤทัย.md` (10 ประโยค)
- ทดสอบ: build (7 routes) + Playwright (เข้าโหมด→แตะเรียง→ตรวจจับผิดได้จริง→เฉลยโผล่) ไม่มี console error
- หมายเหตุ: ตั้งใจ "ตรวจให้จริง" (ไม่ปล่อยบั๊กแบบ mockup อาจารย์ที่เรียงคำแล้วไม่ตรวจ)