---
tags: [บันทึกงาน]
updated: 2026-08-02
---

# บันทึกงาน 2 ส.ค. 2026

- pull/push เช้า: ได้โน้ตบอล "สูตรความพร้อมจากการเรียน" (cd6edfb) · เครื่องหฤทัยไม่มีงานค้าง
- รันเว็บในเครื่องตามแผน SETUP.md (โหมดเครื่องเบา: `npm run dev` อย่างเดียว ชี้ API ไป Render) — เว็บขึ้นใน ~1 วิ ไล่ดูครบ 7 หน้าบนจอมือถือ 430px
- ✅ หน้าโครง/ข้อมูล local ขึ้นครบสวย: หน้าแรก · /progress (Dexie) · /profile · /roadmap (โครง 5 หมวดถูกต้อง)
- ⚠️ หน้าที่พึ่งคลังคำค้างโหลด: /flashcards · /practice · /review — **ไม่ใช่บั๊กโค้ด** แต่เพราะ…
- 🔴 **พบปัญหาใหญ่: โดเมน Supabase (`cvjsuqmmiagsfgsufviw.supabase.co`) หายจาก DNS** — เช็กแล้วทั้ง 1.1.1.1 / 8.8.8.8 = NXDOMAIN ไม่ใช่เน็ตฝั่งเรา · สาเหตุน่าจะ **โปรเจกต์ฟรี tier ถูก pause อัตโนมัติ** (ทีมไม่มีใครแตะระบบ 7 วันพอดี — 27 ก.ค. → 2 ส.ค.)
- ผลกระทบ: **เว็บ production บน Vercel ก็โหลดคำศัพท์ไม่ได้เหมือนกัน** (ใช้ Supabase ตัวเดียวกัน) — ใครเปิดแอปตอนนี้จะเห็นบัตรคำ/ฝึก/ทวนค้างโหลดหมด
- 📌 งานถึงบอล (ต้องบัญชี Supabase ของบอล): เข้า dashboard → Restore/Unpause โปรเจกต์ `icts-thai` → เช็กว่าตาราง words + ไฟล์เสียงยังอยู่ · กันเกิดซ้ำ: ตั้ง cron ping ฟรี (เช่น GitHub Actions ยิง query เบา ๆ ทุก 2-3 วัน) หรืออัปเกรด/ย้ายตอน midterm ใกล้ ๆ
- ข้อสังเกตออกแบบ (ยังไม่แก้ — รอคุยบอล): ทุกหน้าเรียนพึ่ง Supabase ตรง ๆ ไม่มี fallback/สถานะ error — ถ้าหลังบ้านล่มผู้เรียนเห็น "กำลังโหลด…" ค้างตลอด ควรมี timeout + ข้อความบอก + (ในอนาคต) cache คำใน Dexie ให้ใช้ออฟไลน์ตามคอนเซ็ปต์ PWA
